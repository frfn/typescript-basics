{
	"name": "udemytypescript",
	"version": "1.0.0",
	"description": "typescript basics",
	"main": "dist/15. mapped types/index.js",
	"scripts": {
		"dev": "concurrently -k -n ts,nodemon -c magenta,cyan \"tsc -w\" \"nodemon -w dist -q dist/15. mapped types/index.js\"",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/react": "^17.0.2",
		"@types/react-dom": "^17.0.1",
		"concurrently": "^5.3.0",
		"nodemon": "^2.0.7",
		"react-dom": "^17.0.1",
		"typescript": "^4.1.3"
	},
	"dependencies": {
		"apollo-client": "^2.6.10",
		"react": "^17.0.1"
	}
}
