{
	"name": "udemytypescript",
	"version": "1.0.0",
	"description": "typescript basics",
	"main": "dist/11. alias/alias.js",
	"scripts": {
		"dev": "concurrently -k -n ts,nodemon -c magenta,cyan \"tsc -w\" \"nodemon -w dist -q dist/11. alias/alias.js\"",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^5.3.0",
		"nodemon": "^2.0.7",
		"typescript": "^4.1.3"
	}
}
