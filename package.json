{
	"name": "udemytypescript",
	"version": "1.0.0",
	"description": "typescript basics",
	"main": "index.js",
	"scripts": {
		"dev": "concurrently -k -n TSC,NODEMON -c yellow,green \"tsc -w\" \"nodemon -w dist -q dist/3. interfaces/interface.js\"",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^5.3.0",
		"nodemon": "^2.0.7",
		"typescript": "^4.1.3"
	}
}
